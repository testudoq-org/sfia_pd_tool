{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "grunt build",
            "type": "shell",
            "command": "grunt build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
